/**
 * Firebase (Firestore). Uses real implementation when the package is installed.
 * If you see "Bookings cannot be saved until Firebase is connected", run: npm install
 */
export * from './firebase.real';

